# VRS Manager Requirements
# Python 3.7+

# Data processing
pandas>=1.3.0
numpy>=1.21.0

# Excel file handling
openpyxl>=3.0.0

# GUI (tkinter is included with Python, but listed for reference)
# tkinter is part of the Python standard library

# BERT and ML dependencies for StrOrigin Analysis (Phase 2.3)
# NOTE: sentence-transformers 5.0+ required for bundled model compatibility
torch>=2.0.0
transformers>=4.30.0
sentence-transformers>=5.0.0
tokenizers>=0.13.0
scipy>=1.7.0
scikit-learn>=1.0.0
tqdm>=4.62.0
regex>=2021.8.3
requests>=2.26.0
packaging>=21.0
filelock>=3.4.0
huggingface-hub>=0.10.0
